# 6. Service to expose the 'hpdb' application internally.
apiVersion: v1
kind: Service
metadata:
  name: hpdb-app-service
  labels:
    app: hpdb
    tier: frontend
spec:
  ports:
  - name: 8080-tcp
    port: 8080
    targetPort: 8080
  selector:
    app: hpdb
    tier: frontend