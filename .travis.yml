language: java

git:
  depth: 3

jdk:
  - oraclejdk8

services:
  - mysql
  
before_install:
  - echo "MAVEN_OPTS='-Xmx512m'" > ~/.mavenrc  
  
before_script:
  - mysql -e 'create database hpdbweb;'
  - "echo $JAVA_OPTS"
  - "echo $MAVEN_OPTS"

sudo: false
script: mvn clean install