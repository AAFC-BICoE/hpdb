language: java

git:
  depth: 3

jdk:
  - oraclejdk8

services:
  - mysql
  
before_script:
  - mysql -e 'create database hpdbweb;'
  - "echo $JAVA_OPTS"
  - "echo $MAVEN_OPTS"
  - ulimit -a
  - free -m

after_script:
  - ulimit -a
  - free -m

sudo: false
script: mvn clean install